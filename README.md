# A* Comparative Visualizer

## Overview
An interactive web application to visualize the A* search algorithm using four heuristics simultaneously.

## Tech Stack
- Frontend: React + Vite + Canvas
- Backend: Flask + Python

## Setup
1. Backend: `cd backend && pip install -r requirements.txt && python app.py`
2. Frontend: `cd frontend && npm install && npm run dev`
